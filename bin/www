#!/usr/bin/env node
// PaaS entrypoint. The platform launches:
//   node ./bin/www --host 0.0.0.0 --port 3000
// The --host/--port flags (and PORT/HOST env) are honored in lib/config.js.
import '../index.js';
